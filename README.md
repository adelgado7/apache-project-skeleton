# apache-project-skeleton
Interactive Bash script that scaffolds Apache-based web projects using PHP, MariaDB, and Bootstrap. Detects and installs PHP-FPM, generates secure project directories, starter files, nginx configs, and environment/version metadata for fast, repeatable server setup.
